# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/

# Virtual envs (uv-managed)
.venv/
venv/
env/
.env

# uv
# uv.lock IS committed (reproducible installs); only the venv is ignored.

# Testing / coverage
.coverage
.coverage.*
coverage.xml
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# agent pipeline handoff (transient)
.pipeline/

# Playwright MCP browser artifacts (transient)
.playwright-mcp/

# Ingested corpus (M4-2, #283) — corpus-derived text is private and is never committed;
# committed storage is M4-5 (#286). See docs/CORPUS_LEDGER.md.
apps/quality_database/.corpus_out/
